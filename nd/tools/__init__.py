"""Read-only tool functions grouped by ND domain.

Each function takes an `NdClient` and returns a compact, guarded string. The MCP
server exposes these as tools; tests and pipelines call them directly.
"""
